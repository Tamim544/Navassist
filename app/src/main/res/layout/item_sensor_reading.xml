<?xml version="1.0" encoding="utf-8"?>
<!--
    Item layout for the History RecyclerView.
    Each row: timestamp | distance | LDR | light level | activity
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="4dp"
    android:layout_marginVertical="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/surface">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Timestamp -->
        <TextView
            android:id="@+id/tv_timestamp"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/on_surface_secondary"
            android:text="—"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/tv_activity_history"
            android:contentDescription="Reading timestamp" />

        <!-- Activity label (top right) -->
        <TextView
            android:id="@+id/tv_activity_history"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/primary"
            android:text="—"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:contentDescription="Activity label" />

        <!-- Distance -->
        <TextView
            android:id="@+id/tv_distance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/on_surface"
            android:text="—"
            app:layout_constraintTop_toBottomOf="@id/tv_timestamp"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="4dp"
            android:contentDescription="Distance value" />

        <!-- LDR value -->
        <TextView
            android:id="@+id/tv_ldr_history"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@color/on_surface_secondary"
            android:text="LDR: 0"
            app:layout_constraintTop_toBottomOf="@id/tv_distance"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="2dp"
            android:contentDescription="LDR raw value" />

        <!-- Light level -->
        <TextView
            android:id="@+id/tv_light_history"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@color/on_surface_secondary"
            android:text="—"
            app:layout_constraintTop_toBottomOf="@id/tv_distance"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="2dp"
            android:contentDescription="Light level" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
